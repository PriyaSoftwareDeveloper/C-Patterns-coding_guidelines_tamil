#include <stdio.h>
#include <stdlib.h>

int main(void){
	int n,i,j;
	setbuf(stdout, NULL);
	printf("Enter n number: ");
	scanf("%d", &n);
	for (i=0; i<n; i++){
		for(j=i+1; j<n; j++){
			printf("$ ");
		}
		printf("\n");
	}
	return 0;
}
