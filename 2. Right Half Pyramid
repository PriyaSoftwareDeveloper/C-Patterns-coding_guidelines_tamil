#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int i, j;
	setbuf(stdout, NULL);
	for(i=0; i<=5; i++){
		for(j=1; j<=i; j++){
			printf("* ");
		}
		printf("\n");
	}
	return EXIT_SUCCESS;
}
